# Generated by Droid

auto lo
iface lo inet loopback

auto eth0 {% if host.private_ip %}eth0:0{% endif %}

iface eth0 inet static
    address {{ host.public_ip }}/24
{% if gateway_ip is defined %}
    gateway {{ gateway_ip }}
{% endif %}
    dns-nameservers{% for dns_server in mod_networking_dns_servers %} {{ dns_server }}{% endfor %}
    dns-options rotate

{% if host.private_ip %}
iface eth0:0 inet static
    address {{ host.private_ip }}/17
{% endif %}
